name: todolist_cicd
on:
  push:
     branches: [main]
jobs:
  todolist_cicd:
     runs-on: self-hosted
     steps:
       - uses: actions/checkout@v2
       - name: copy file to /projects/todolist
         script:
           cp -rf * /projects/todolist
           cd /projects/todolist
           pwd
       
