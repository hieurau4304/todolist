name: todolist_cicd
on:
  push:
    branches: [main]
jobs:
   todolist_cicd:
     runs-on: self-hosted
     steps:
       - uses: actions/checkout@v4
       - run: pwd
       - run: sudo cp -rf * /github/github_action/todolist
           
         
        
