name: todolist_cicd
on:
  push:
    branches: [main]
jobs:
  todolist_cicd:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4 # Checkout the repository
      - name: Run command in subfolder
        working-directory: /projects/todolist
        run: pwd

        

